# BillEase — Dependency Reference
#
# This is a Node.js / npm project, not Python — nothing installs from this
# file. It exists purely as a quick-reference list of the packages this app
# depends on. The actual source of truth is package.json (exact resolved
# versions are locked in package-lock.json).
#
# To install everything, run from the project root:
#   npm install

# --- Runtime dependencies ---
lucide-react==1.16.0      # icon set used throughout the UI
react==18.3.1             # UI framework
react-dom==18.3.1         # React renderer for the browser

# --- Development dependencies ---
@types/react==18.3.31         # TypeScript types for React
@types/react-dom==18.3.7      # TypeScript types for react-dom
@vitejs/plugin-react==4.3.4   # Vite plugin: React Fast Refresh + JSX
typescript==5.6.2             # Type-checking (tsc --noEmit) before every build
vite==6.0.7                   # Dev server & production bundler

# --- Runtime requirement ---
node>=18.0.0               # Node.js 18 or newer required to run npm/Vite
